name: Security Lint

on: [push, pull_request]

jobs:
  no-raw-exec:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python tools/lint_no_raw_exec.py src/
